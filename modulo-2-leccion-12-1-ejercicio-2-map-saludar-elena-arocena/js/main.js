"use strict";

const names = ['María', 'Lucía', 'Susana', 'Rocío', 'Inmaculada'];
const welcome = 'Bienvenida, '

const namesWelcome = names.map(names => welcome + names);

console.log(namesWelcome);